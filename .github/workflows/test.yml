name: Run Tests

# When to run this workflow
on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

# Jobs to run
jobs:
  test:
    runs-on: ubuntu-latest
    
    # Service containers (PostgreSQL and Redis)
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: urlshort_user
          POSTGRES_PASSWORD: odysbase
          POSTGRES_DB: urlshortener_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
      
      redis:
        image: redis:7-alpine
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 6379:6379
    
    steps:
      # Step 1: Get your code
      - name: Checkout code
        uses: actions/checkout@v4
      
      # Step 2: Set up Python
      - name: Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'  # Cache pip dependencies for faster builds
      
      # Step 3: Install dependencies
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      # Step 4: Run tests
      - name: Run tests with pytest
        env:
          DATABASE_URL: postgresql://urlshort_user:odysbase@localhost:5432/urlshortener_test
          TEST_DATABASE_URL: postgresql://urlshort_user:odysbase@localhost:5432/urlshortener_test
          SECRET_KEY: test-secret-key-for-ci-pipeline-only
          ALGORITHM: HS256
          ACCESS_TOKEN_EXPIRE_MINUTES: 60
          TESTING: true
          REDIS_HOST: localhost
          REDIS_PORT: 6379
        run: |
          pytest -v --tb=short